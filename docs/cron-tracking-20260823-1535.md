# 5 实例追踪 2026-08-23 15:35 (cron 周期)

## 关键发现：01/02 是自然完成停机，不是 Bug

**实例存活（2/5，符合用户策略 1-2 实例）**：
- 03 systemd active (PID 9686, 6:50 etime) ✓ 探针 15s 内被消费
- 05 systemd active (PID 3073, 37:31 etime) ✓ 探针 15s 内被消费

**实例未运行（3/5）**：
- 01 systemd 14:57:26 Stopped — 原因: campaign work_8cea46a01eec `report delivered` 后 STOP_PROJECT 自然退出
- 02 systemd 14:26:45 Stopped — 原因: campaign work_5ae596fb9a0c `molecular data unavailable` 后 STOP_PROJECT 自然退出  
- 04 systemd disabled / inactive (长期)

**partner-campaign scheduler 确认**：active_instances = ["03", "05"]，系统状态一致

## 自进化机制层评估

**主事件循环（heartbeat / inbox 消费 / api_calls）**：
- 03/05 正常：inbox 消费 + DirectAPI classify/action 持续调用
- 01/02 已停机，但 **campaign.py 持续运行**（PID 2451），定期 dispatch 给 03/05 → 主调度机制正常

**任务执行层**：
- 03 当前 task 2c4531fb-4b6a-4925-ab69-2ff8cc74339a 处于 STEP 1/4 (write_design)，调用 DirectAPI timeout=600s
- 05 当前 task 7e7415eb 处于 step_retry 循环 + robust_execute_success → 重试-成功路径工作正常
- api_calls 23 次 last-hour 全 status=ok → 模型层正常

**演进事件层（evolution.jsonl）**：
- last 15 min 仅 6 条事件（03 占 5 条，1 条 inst=''），相比之前活跃度大幅下降
- 39% instance='' 比例维持（Bug #29.1 v1.1.0 修复后基线 ~42.9%）
- 没有新发现机制层 bug

**已知机制层问题（不需重复汇报）**：
- Bug #27: Restart=on-failure 不拉起干净退出实例（01/02 自然退出被 systemd 不拉是预期）
- Bug #29.1: inst='' 比例 ~39%（基线 42.9%，不是新 bug）
- Bug #32/#37: TASK_FAILED handler 不触发 iter chain（暂未在本轮观察到）

## 唤起用户

**不唤起**：
- 03/05 活跃 → 符合用户资源策略"1-2 实例"
- 01/02 是 campaign 任务自然完成后停机，不是机制层 bug
- 用户已知 #27 不再重复提醒

## 后续追踪

- 下一轮 cron 10min 后再查
- 重点观察 03 write_design → strict_reflect → next_iteration 闭环是否顺畅
- 05 step_retry 多次重试 → 关注是否形成死循环（8 次 step_retry + 3 次 step_completed = 重试-成功交替）
