# 02 分子生成项目状态

## 历史四轮

1. 生成并评估 85 个有效候选。
2. 计算 Bemis–Murcko 骨架多样性和 Morgan 指纹两两相似度。
3. 计算 SA，与随机种子 20260822 的等样本基线对照。
4. 以 0.6 QED + 0.4 ease-of-synthesis 选择 Top-20 并检查骨架集中。

## 历史证据

- 规则组：85/85 唯一，平均 QED 0.537781，平均 SA 2.174044。
- 随机基线：55/85 唯一，平均 QED 0.533471，平均 SA 2.230464。
- Top-20：9 个唯一结构、4 个骨架，平均 QED 0.598151，平均 SA 1.457814。

## 旧结论纠正

多目标排序改善了头部 QED/SA，但造成明显结构集中，因此仍不重复旧排序，也不宣称真实药效优化。
但“当前没有目标活性、分子对接数据”的结论已经被真实 TargetDiff 文件推翻：184087 条记录中
76803 条 `pk>0`，同一记录包含 `pk/vina/rmsd`。错误收据已追加失效，不删除历史。

## Sprint 12 当前主线

1. 固定 `pk` 标签和 `vina/rmsd` 特征合同。
2. 用 key 首段近似靶点组做确定性五折，训练/测试组重叠为零。
3. 比较训练均值、线性和固定 HGB，分析 RMSD 分层与最差靶点组。
4. 对 Vina 异常值做训练折分位数敏感性实验，并用五折预声明门决定是否保留非线性候选。
5. 每一步实际保存运行源码、JSON、详细 PDF 和真实 QQ 回执；05 最后才摄取 RL 轨迹。

Stage 1–13 已完成。持续 canary 自动运行 Stage 9→10→05→11→12→13→05，7/7 completed、
0 failure、0 retry；下一业务 WorkItem 只在上一收据和真实交付完成后产生。

## 当前边界和下一步

配体聚合后 HGB 五折平均改善 0.0592；靶点等权改善 0.0403，内部 bootstrap 95% CI
[-0.0683,-0.0148]。三个预注册门通过，因此保留 HGB candidate，但不生产晋升。

## 官方 CrossDocked split 对照（2026-08-24）

作者仓库 README 要求 `crossdocked_pocket10_pose_split.pt`，并说明它由与 AR/Pocket2Mol 共用的
`split_by_name.pt` 生成。作者 Google Drive 当前不可访问，因此使用 Zenodo 记录 17107488 的公开镜像；
文件 MD5 为 `d782da9499096612ca7115cb94313aa2`，大小 15,284,527 B。来源级别是“校验和一致的公开镜像”，不得写成作者 Drive 直接下载。

安全结构审计得到 train/test identity 100,000/100，靶点组 1,907/93 且交集为零。与 affinity_info
精确匹配且正 pK 的有效建模行只有 40,617/27；线性 RMSE 2.2678，HGB 2.2181。靶点组 bootstrap
95% CI `[-0.2084,0.1139]` 跨零，因此 Stage 13 的内部五折优势尚未获得官方边界稳健复现，
下一方法决策保持 candidate/inconclusive。
