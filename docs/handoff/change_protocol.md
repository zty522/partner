# 变更协议

1. 记录问题与可复现证据。
2. 确定影响层：上下文、规划、事件、环境、验收、交付、调度或数据。
3. 建立可验证假设，写明修改前基线和成功标准。
4. 将改进作为 candidate，不直接宣称成功。
5. 运行针对性测试、必要回归和小范围实机验收。
6. 输出 `promoted` / `rejected` / `inconclusive` 决策。
7. 晋升后更新能力、反模式、当前状态和回归测试；失败则回退并保留证据。

项目产物和系统改进分开记录。一份项目报告不能作为系统进化的唯一证据。
