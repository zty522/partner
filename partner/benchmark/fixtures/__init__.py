"""Benchmark task family fixtures (M3 / Section 8.1).

State honesty: static_implemented. Each fixture file defines a small,
deterministic multi-stage task that can be graded by a short script.
The fixtures are deliberately synthetic (clearly labelled as such) so
they cannot be confused with real research tasks. Replacing them with
materialised tasks is a separate verification-pass job.
"""
