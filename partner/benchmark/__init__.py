"""Benchmark package for Partner continual improvement study (M3).

State honesty: static_implemented. All schemas and runner entry points
exist on disk but were not exercised in this session; no benchmark was
ever run. Real runs require an explicit benchmark workspace separate
from the production workspace.
"""
